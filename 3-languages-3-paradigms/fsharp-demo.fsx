// Primitive types
1
true
"Hello"

// Compound type: tuple
let twoTuple = (true, "Hello")
let threeTuple = (1, "Two", 3)

// Compound type: list
let list = [1; 2; 3]


// Compound type: record
type User = { active: bool; name: string }
let record = { active =  true; name = "John" }

// Compound type: record
type DiscriminatedUnion = 
    | 

// Compound type: option


// Assignment (binding)
let name = "F# Demo"

// Functions
let double x = x * x
let add x y = x + y

// Nested functions
// TODO

// Significant whitespace
let triple x =
    x * x * x
    
// Functions as first-class citizens
 
 
// Pure functions

// Recursion

// Immutability

// Assignment (binding)
let age = 43
age <- 44

// TODO: records

// Pattern matching

// Deconstruction

// Expressive type system

// Type inference

// Lists
